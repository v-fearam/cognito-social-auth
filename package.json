{
  "name": "cognito-social-auth",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev -w @csa/frontend",
    "dev:backend": "npm run start:dev -w @csa/backend",
    "build": "npm run build --workspaces"
  },
  "engines": {
    "node": ">=20"
  }
}